@model TRADENET.LedgerReport4
@{ 
    Layout = null;
}

@Html.DevExpress().WebDocumentViewer(settings =>
{
    settings.Name = "WebDocumentViewer";
    settings.DisableHttpHandlerValidation = true;
    settings.ClientSideEvents.Init = "function(s,e){  s.GetPreviewModel().reportPreview.zoom(1.5);}";  
}).Bind(Model).GetHtml()


